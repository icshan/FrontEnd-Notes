/**
 * 
 * @param {*} strs 
 * 题目：给定一个 n 个元素有序的（升序）整型数组 nums 和一个目标值 target  ，写一个函数搜索 nums 中的 target，如果目标值存在返回下标，否则返回 -1。
 * 解题思路：
 * 1. 设定left=0，right = length -1，nid = left = left + （right - left）/ 2 
 * 2. 当left <= 
 *
 */
function binarySearch(){

}